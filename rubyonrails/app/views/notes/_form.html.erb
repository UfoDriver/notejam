<%= form_for(@note, html: {class: 'note'}) do |f| %>
  <% if @note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@note.errors.count, "error") %> prohibited this note from being saved:</h2>

      <ul>
      <% @note.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.label :name %><br />
  <%= f.text_field :name %>

  <%= f.label :text %><br />
  <%= f.text_area :text %>

  <%= f.label :pad %>
  <%= f.select(:pad_id, current_user.pads.all.collect {|p| [p.name, p.id]}, include_blank: true) %>

  <%= f.submit %>
<% end %>

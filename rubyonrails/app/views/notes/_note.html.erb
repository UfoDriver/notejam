<tr>
  <td><%= link_to note.name, note_path(note) %></td>
  <td>
    <% if note.pad.nil? %>
      No pad
    <% else %>
      <%= link_to note.pad.name, pad_path(note.pad) %>
    <% end %>
  </td>
  <td><%= distance_of_time_in_words_to_now note.updated_at %> ago</td>
</tr>
